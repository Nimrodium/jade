/*
 * Lock file, keeps pkgname(url) -> hash
 * used for resolving store paths, if it cannot locate the path in the lock file, or the lock file does not exist, then it assumes it does not exist, and downloads from the package source pool
 *
 */
